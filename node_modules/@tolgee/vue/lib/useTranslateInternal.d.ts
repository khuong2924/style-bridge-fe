import { TranslateProps, NsFallback } from '@tolgee/web';
import type { Ref } from 'vue';
export declare const useTranslateInternal: (ns?: NsFallback) => {
    t: Ref<(props: TranslateProps<any>) => any>;
    isLoading: Ref<boolean>;
};
